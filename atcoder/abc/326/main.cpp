#include <bits/stdc++.h>

#ifdef LOCAL
#include "debug.h"
#else
#define dbg(...) 42
#endif

void solve() {
  constexpr std::string_view abc{"ABC"};

  int n;
  std::cin >> n;
  std::string r, c;
  std::cin >> r >> c;


  std::cout << "No\n";
}

int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);
  std::cout.tie(nullptr);

  solve();
}